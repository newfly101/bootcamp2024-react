import React from 'react';

const JoinInput = (props) => {
    let flag = props.join;
    console.log("회원가입 버튼 누름 !", flag);
    return (
        <div>

        </div>
    );
};

export default JoinInput;